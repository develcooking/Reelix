import {AboutSlint, TextEdit, LineEdit,TabWidget,ScrollView,Palette,StyleMetrics, ComboBox, VerticalBox, CheckBox, GroupBox, HorizontalBox, Button, StandardTableView, GridBox } from "std-widgets.slint";
import { ComboBoxScroll } from "ComboBoxScroll.slint";
import { SideBar } from "side_bar.slint";
import { RecordPage, RequestPage, StatisticsPage, AnalyzesPage, CategoriesPage, SettingsPage, AboutPage } from "pages/pages.slint";

export global Logic {
     callback open-url(string);
     callback cabavalueofcombobox(string);
     callback createtype(string);
}
export component AboutPage {


    VerticalBox {
        Text {
            text: @tr("Product: Reelix (Working title)\nVersion: 0.0.2 (extreme Alpha)");
        }                    Text {
            text: @tr("Developed by: Jan-Peter Friebe\n");
        }
        Text {
            text: "https://gitlab.rz.uni-bamberg.de/ba4ju8/reelix/";
            color: lightblue;

            TouchArea { clicked => { Logic.open-url(parent.text); } }
        }
        AboutSlint {
            max-width: 256px;
            max-height: 256px;
            preferred-width: 128px;
            preferred-height: 128px;
        }
    }
}

export component MainWindow inherits Window {

    in property <[string]> the_model;
    
    height: 420px;
    width: 820px;
    title: @tr("Reelix");
    icon: @image-url("../images/Reelix.png");


    HorizontalLayout {
        side-bar := SideBar {
            title: @tr("✨Reelix");
            model: [@tr("Menu" => "Record"), @tr("Menu" => "Request-Log"), @tr("Menu" => "Statistics"), @tr("Menu" => "Analyzes"), @tr("Menu" => "Manage Categories"),@tr("Menu" => "Settings"), @tr("Menu" => "About")];
        }
        if(side-bar.current-item == 0) : RecordPage {}
        if(side-bar.current-item == 1) : RequestPage {}
        if(side-bar.current-item == 2) : StatisticsPage {}
        if(side-bar.current-item == 3) : AnalyzesPage {}
        if(side-bar.current-item == 4) : CategoriesPage {}
        if(side-bar.current-item == 5) : SettingsPage {}
        if(side-bar.current-item == 6) : AboutPage {}
    }
}
