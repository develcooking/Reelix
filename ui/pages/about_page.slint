import {AboutSlint, VerticalBox} from "std-widgets.slint";
import { Logic } from "../logics.slint";

export component AboutPage {


    VerticalBox {
        Text {
            text: @tr("Product: Reelix (Working title)\nVersion: 0.0.2 (extreme Alpha)");
        }                    Text {
            text: @tr("Developed by: Jan-Peter Friebe\n");
        }
        Text {
            text: "https://gitlab.rz.uni-bamberg.de/ba4ju8/reelix/";
            color: lightblue;

            TouchArea { clicked => { Logic.open-url(parent.text); } }
        }
        AboutSlint {
            max-width: 256px;
            max-height: 256px;
            preferred-width: 128px;
            preferred-height: 128px;
        }
    }
}