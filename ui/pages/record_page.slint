import {VerticalBox, ComboBox, GroupBox, TextEdit, HorizontalBox, LineEdit, Button} from "std-widgets.slint";
import { Logic } from "../logics.slint";

export component RecordPage inherits Rectangle {
    in property <[string]> the_model;
   HorizontalBox {
    alignment: center;    
    VerticalBox {
        alignment: start;
    
        GroupBox {
          
    
            hi := VerticalBox {
                alignment: center;
                spacing: 30px;
    
    
                 ComboBox {
                    width: 560px;
                    height: 40px;
                  
                    model: the_model;
                }            Text {
                    text: @tr("Under Construction doesn't work yet");
                }
    
                }

            }
                LineEdit {
                text: "";
                placeholder-text: "For inquiries regarding forms, room and personal information and lost property.";
                
                preferred-height: root.height;
            }
            HorizontalBox {
            Button {
                text: "Date";
            }
            Button {
                text: "Save Record";
                primary: true;
            }
        }
}
}   
      
    }